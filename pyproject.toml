[project]
name = "network-security-monitoring-lab"
version = "0.1.0"
description = "Portfolio network security log monitoring CLI"
requires-python = ">=3.10"

[project.scripts]
netmon = "netmon.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
